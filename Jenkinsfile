pipeline {
    agent any 
    environment {
        HOMEE = "Boom"
    }
    stages {  
        stage('Git Checkout') {
            steps {
                echo "My 1st Test! ${HOMEE}"
            }
        }
    }
}
